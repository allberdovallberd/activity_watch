# Institution Usage Tracking

Project layout:

- `backend/` - API + database
- `android-app/` - tablet collector APK
- `web-app/` - admin dashboard

Current status:

- Backend (PostgreSQL): category hierarchy + device CRUD + binding enforcement.
- Android app: permission-gated auto collector with manual device ID linking.
- Web app: admin login + main/sub category management + device management + analytics.
